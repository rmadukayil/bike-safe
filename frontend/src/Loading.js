import React from 'react';
import ReactLoading from 'react-loading';

const loadingIcon = () => (
  <ReactLoading type={'spin'} color={'#D9B310'} height={'20%'} width={'20%'} />
);

export default loadingIcon;